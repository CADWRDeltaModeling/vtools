

Installing instruction:

To install regular vtools package:

   a. first run the py_installer.bat within each package folders to create setup eggs
   b. navigate to the folder which contain the corresponding egg file and use command 
      easy_install packageeggsfile to install a vtool package.
      
To install vtools package for developer:

   a. first run the py_installer_develop.bat within each package folders to install a package.
